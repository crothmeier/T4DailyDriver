apiVersion: v1
kind: Service
metadata:
  name: vllm-service-nodeport
  namespace: vllm
  labels:
    app: vllm
    model: mistral-7b-awq
spec:
  type: NodePort
  selector:
    app: vllm
    model: mistral-7b-awq
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
